_base_: common.yaml

data:
  backend: "packed_memmap"
  packed_manifest: "data/packed_tc_memmap/manifest.json"
  batch_size: 8

model:
  attention_resolutions: [32]
  attention_heads: 4

evaluation:
  guidance_scale: 2.5
