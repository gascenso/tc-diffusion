experiment:
  name: "baseline_ddpm_tc"
  output_dir: "runs/baseline_ddpm_tc"

data:
  gridsat_dir: "data/gridsat_patches"
  batch_size: 4
  num_workers: 2
  image_size: 256
  bt_min_k: 117.0
  bt_max_k: 348.0

conditioning:
  num_ss_classes: 6           # 0-5 Saffir-Simpson categories
  use_wind_speed: false       # we'll wire this later
  use_basin: false            # placeholder

model:
  base_channels: 16
  channel_mults: [1, 2, 4]
  num_res_blocks: 2
  attention_resolutions: []   # no attention yet
  dropout: 0.0

diffusion:
  num_steps: 1000
  beta_schedule: "linear"
  loss_type: "eps_mse"

training:
  num_epochs: 25
  lr: 1e-4
  ema_decay: 0.9999
  log_interval_steps: 10
  mixed_precision: false
