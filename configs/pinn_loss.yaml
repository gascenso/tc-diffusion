_base_: local.yaml

physics_loss:
  enabled: true
  lambda_phys: 0.015
  lambda_phys_warmup_frac: 0.4
  radial_weight: 1.0
  cold_weight: 0.0
  grad_weight: 0.25
  snr_gate_max: 0.4
  radial_bins: 64
  cold_threshold_k: 200.0
  cold_softness_k: 5.0
  charbonnier_eps: 1.0e-3

training:
  num_epochs: 160
